export interface Livestream {
	id: string;
	title: string;
	videoId: string;
	start: string;
	end: string;
}

export interface News {
	id: string;
	title: string;
	content: string;
	posted: string;
}
